import {Meta, Story, Preview, Props} from '@storybook/addon-docs/blocks'
import {action, decorate} from '@storybook/addon-actions'
import {withKnobs, text, boolean, number} from '@storybook/addon-knobs'
import Button from './Button'

<Meta title="Buttons" component={Button} decorators={[withKnobs]} />

# Button

Use a `Button` for user initiated actions.

## Props

<Props of={Button} />

## Example Usage

```tsx
<Button disabled label="Click me!" variant="primary" onClick={() => {}} />
```

<Preview>
  <Story name="Default">
    <React.Fragment>
      <Button
        disabled={boolean('Disable', false)}
        label={text('Label', 'Click me!')}
        variant="primary"
      />
      &nbsp;
      <Button disabled={boolean('Disable')} label={text('Label')} variant="outline" />
      &nbsp;
      <Button disabled={boolean('Disable')} label={text('Label')} variant="default" />
      &nbsp;
      <Button
        disabled={boolean('Disable')}
        variant="sharp"
        onClick={action('click')}
        label="45"
        icon="coin"
      />
    </React.Fragment>
  </Story>
</Preview>
