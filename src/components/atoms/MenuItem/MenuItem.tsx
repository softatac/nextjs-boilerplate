import React from 'react'

const MenuItemContainer = ({children}) => (
  <div className="flex justify-center lg:inline-block w-full lg:w-auto lg:h-full lg:text-lg relative">
    {children}
  </div>
)

export default MenuItemContainer
